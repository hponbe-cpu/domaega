# app

## Setup
1. `npm install`
2. Fill `.env.local` with Supabase credentials (dashboard → Settings → API).
3. `npm run dev`

## Deploy
- Push to GitHub, import on Vercel. Env vars copied from `.env.local`.
